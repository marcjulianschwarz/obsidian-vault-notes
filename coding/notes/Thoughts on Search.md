
- Prefer searching many small indices instead of few big indices 
	- e.g. by pre-filtering to select a smaller index to search through
	- create indices with possibly some redundancy (more storage costs) for better performance (less compute cost)
